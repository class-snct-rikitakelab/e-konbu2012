#ifndef DETECTLINEOUT_DETECT_LINE_STATE_H
#define DETECTLINEOUT_DETECT_LINE_STATE_H

#include <string>
#include <vector>
#include <list>
#include <iostream>
#include <assert.h>

namespace DetectLineOut
{
typedef enum 
{
	OnLine,
	OutOfLine
} DetectLineState;

}  // namespace DetectLineOut
#endif
